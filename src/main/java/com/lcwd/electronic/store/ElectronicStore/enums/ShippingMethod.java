package com.lcwd.electronic.store.ElectronicStore.enums;

public enum ShippingMethod {
    NORMAL, // the NORMAL delivery is (today + 5 days)
    EXPRESS, // means (today + 3 days)
    PRIME // means (today + 2 days)
}
